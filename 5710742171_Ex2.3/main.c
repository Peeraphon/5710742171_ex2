#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a;
    scanf("%d",&a);
    if((a>40) && (a<=100))
    {
        printf("in rang41-100");
    }
    else
    {
        printf("not in rang 41-100");
    }

    return 0;
}
